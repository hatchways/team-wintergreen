import { Grid } from '@mui/material';

const Messages = () => {
  <Grid container maxHeight="100vh">
    <Grid item xs={4}></Grid>
    <Grid item xs={6}></Grid>
  </Grid>;
};

export default Messages;
